# 核心闭环 UI 重构进度

截至 2026-07-23，按已确认计划核验：

| 阶段 | 状态 | 证据与边界 |
| --- | --- | --- |
| 一：视觉基线 | 已完成（当前存档可达范围） | 固定 `1440 × 900` 基线、页面观察和规格已落在 `docs/ui-baseline/` 与 `docs/ui-visual-spec.md`；需在阶段六补含草案/长文本的专用存档截图 |
| 二：原创视觉资产包 | 已完成 | 原创 WebP 纸纹、场景衬景、印记、图标及资产清单已交付；详见 `docs/ui-asset-manifest.md` |
| 三：设计系统与技术骨架 | 已完成（阶段五清理另计） | `main.tsx` 只加载 `styles/index.css`；旧巨型 `styles.css` 已按基础、地图、廷议、人物/密谈、浮层归档为 `styles/legacy/`，运行时不再直接导入。`revival.css` 已迁入组件层的 `core-loop.css`，不再作为末尾覆盖层；令牌、密度、通用组件和核心场景模块均有唯一归属。非核心页面的逐页替换与遗留规则删除属于阶段五。 |
| 四：核心场景重做 | 地图主舞台重构中 | 内容模块已接入核心组件；但不再以“本月要议”或任何独立页面为主操作面。`StrategicMap` 已常驻为底层，当前完成地图案头与右侧地方档案抽屉；廷议、密谈、方略、月报仍需继续收敛为地图上的有限宽度工作窗。 |
| 五：通用浮层与其余页面 | 实现完成，待截图清理 | 军队、城池、人物、外交、史册、事件已迁入 `AppFrame`/`PaperPanel`；API 配置、显示设置、`PanelRoot`、人物筛选、人物召对与地图信息抽屉已迁入 `GameDialog`。遗留样式删除必须在阶段六截图验证后进行。 |
| 六：验证与验收 | 进行中，受活存档阻塞 | 已有构建、37 条契约测试、减少动效、焦点对话框和核心场景主行动契约；后端 8010 当前未运行，且首次启动会创建新存档，故未擅自启动。固定分辨率/窄桌面截图、键盘实测、极端状态与遗留样式删除待用户提供可用存档或明确授权启动。 |

## 下一阶段

继续阶段四：以地图为唯一主操作台，将廷议、密谈、军府方略、月报及其余信息模块从独立场景收敛为有限宽度的地图工作窗；每批均保留地图可见、可点与地方档案上下文。

## 2026-07-23 本批验收

- `DirectiveBook`、`MonthlySummary` 均以 1440×900 活存档完成浏览器截图检查；月结确认框已验证可见且未执行不可逆月结。
- 1024×768 月报初检发现全局 `body` 的历史 `1160px` 下限造成横向溢出；已迁至设计系统的 `min-width: 0`，复测 `documentWidth = viewportWidth = 1024`。
- 修复草案/颁令批次客户端漏写 `/api` 前缀，避免 Vite 开发页返回 HTML 后触发 JSON 解析错误。
- 前端契约测试 35/35 通过；本批增加“入口不重新加载旧巨型样式/末尾覆盖层、裁决页不再内嵌样式”的契约。完整构建通过。

## 2026-07-23 阶段四收口

- `StrategicMap` 不再渲染 `province-detail-panel`、`commandery-detail-panel`、`city-detail-panel` 或右键快捷菜单；选中州会回流一个可用节点，郡/城节点直接回流 `MapDetail` 地方档案。
- `CouncilHall`、`SecretChat`、`ReviewAndDecree` 均改由 `AppFrame` 承担场景标题与返回操作；建议库、人物选择和批次信息使用 `PaperPanel`、`SectionHeading`、`ActionSealButton`、`StatusMark` 收束层级。
- 廷议选人页补齐“群臣名录—本月议题—本次议席”三栏，地图档案补齐“概况—相关人物—驻军与军压—军府处置”。
- `npm run build` 与 `node --test tests/*.test.ts` 均通过（36/36）。本地前端可启动，但此时后端活存档接口返回 `Internal Server Error`，未新建或改写存档，故本批仅能完成源代码/契约验证；固定分辨率截图、键盘与极端状态检查保留阶段六。

## 2026-07-23 阶段五第一批

- `ArmyInfo`、`CityGovernance`、`CharacterDetail`、`DiplomacyPage`、`HistoryBook`、`EventPage` 使用统一国政横卷、纸面信息区、章节标题与印章行动；所有即时影响仍只会进入方略草案。
- `ApiConfigModal`、`DisplaySettingsPanel`、`PanelRoot` 已改由 `GameDialog` 提供关闭、Esc、焦点管理与统一纸面皮肤。
- 新增 `info-pages.css`，将信息页从旧深色卡片收束到浅宣纸、灰青结构线与唯一主行动；仍未删除 `legacy/` 中的规则。

## 2026-07-23 阶段五第二批

- `CharactersPanel`、人物书的 `DialogueModal` 与 `MapInfoDrawer` 不再自行渲染遮罩、标题栏与关闭按钮，统一由 `GameDialog` 管理；地图抽屉原有经营业务逻辑未改动。
- 前端契约补充这些浮层必须使用 `GameDialog` 的断言，防止新旧浮层皮肤再次分叉。
- 阶段五实现项已收口，但删除 `legacy/` 中旧选择器、长文本/空数据/加载/错误状态截图和键盘验证属于阶段六，不以构建通过替代视觉验收。

## 2026-07-23 阶段六启动记录

- 确认 `http://127.0.0.1:8010/api/game/state` 当前无法连接；根据项目启动约定，未为截图擅自启动会自动创建新存档的后端。
- 补充 `GameDialog` 的 Radix 对话框原语、关闭控件、`prefers-reduced-motion`、焦点可见性与七个核心场景无浏览器 `confirm()` 的前端契约；活存档浏览器验收仍是未完成的独立条件。

## 2026-07-23 阶段六浏览器验收（活存档）

- 经授权启动 `uvicorn web_app:app` 并加载既有 `208年8月 · 第1回合` 存档；未新建游戏、未执行月结、颁令或其他不可逆写入。
- 已采集 `docs/ui-baseline/1440x900/phase6/` 下的局势、地图、廷议选席、密谈选人、方略簿、月报、审阅页和显示设置对话框截图；`1024x768/phase6/` 下采集局势、地图、月报截图。
- 1440×900 与 1024×768 的已测页面均 `scrollWidth === clientWidth`。显示设置对话框实测打开后焦点在对话框内，按 `Esc` 后关闭。
- 对抗性截图发现地图页的 `AppFrame` 未声明为 grid，导致地方档案挤占地图主体；已改为 `.app-frame.map-hub` 高特异性网格规则，修复后 1440×900 地图主体为 `1100 × 792`，1024×768 为 `696 × 660`，并补充防回归契约。
- 仍未覆盖：廷议进行中的长流文本、密谈往返长文本、方略/审阅的非空草案状态、月结确认框与极端数值；这些需要专用测试存档，不能由当前初始存档伪造。

## 2026-07-23 地图主舞台校正

- 接受产品方向校正：地图不是一个与“本月要议”并列的页面，而是玩家获取天下大势、点选州郡城镇、发起对话与推进决策的唯一主操作台；本月要议只保留为左侧轻量案头。
- `GameScreen` 改为所有路由下都常驻真实 `StrategicMap`，初始页面改为地图案头 `MapDesk`；地图州/郡/城点击统一打开地方档案，不再回退到独立 `MapDetail` 页面。
- `MapInfoDrawer` 改用非模态右侧地方档案抽屉：无暗色遮罩、无背景模糊，固定宽度 430px；地图仍保持可见。浏览器实测 208 年 8 月存档：州图层点选青州后打开北海档案，抽屉坐标为右侧 `826–1256px`，且页面不存在遮罩层。
- 本批仅调整前端呈现和导航承载，不改变 AI 活世界、随机性、月度结算或任何世界事实写入规则，故 `AI活世界与随机性架构基线` 无需更新。
- `npm run build` 与 `node --test tests/*.test.ts` 通过（38/38）；新增契约保护“常驻地图串联核心模块”与“地图地方档案使用非模态抽屉”。
- 后续场景统一获得地图工作窗承载：`SceneShell` 在常驻地图层内固定为有限尺寸、可滚动的纸面工作窗；浏览器实测从地图案头进入廷议后，工作窗为 `1101 × 720px`，地图底层仍存在。廷议返回与取消均回到 `map`。
- 第二批收敛：密谈、军府方略、每月总计的返回入口均改为“返回天下舆图”；月结成功后的前端去向也改为 `map`，不再回到局势页。活存档实测密谈与军府方略均以 `1101 × 720px` 工作窗打开，密谈返回后地图案头恢复；未执行不可逆月结以伪造月报终态验证。
- 本批同样仅涉及前端导航与承载层，未改变 AI 活世界、随机性、月度结算或世界事实写入规则，`AI活世界与随机性架构基线` 无需更新；构建通过，前端契约测试为 39/39。

## 2026-07-23 四槽军府方略簿

- 按确认的“每类一份整合草案”重做方略簿：左侧建议库支持勾选并一次归入内政、军事、外交、其他之一；中间四张输入卡始终可见；右侧仅保留“文书整理”和“校验并送审”。
- 归入建议只追加到草案正文，保存仍复用既有草案 CRUD；同类草案只复用本月最新未颁令草案，不会自动删除历史数据。校验和批次审阅仍走原有接口。
- “文书整理”是本地、可编辑的候选稿编排，不调用整局诏书润色服务，不直接下达或改写任何世界事实。AI 活世界、随机性、月度结算规则没有变化，架构基线无需更新。
- 活存档浏览器实测：地图工作窗为 `1101 × 720px`，四类输入槽均在默认高度完整可见，地图持续处于底层；未保存草案、未送审、未执行月结。

## 2026-07-23 军令书一键下达与文书润色

- 军府方略簿进一步收敛为一份连续军令书，内政、军事、外交、其他四节上下相连；左侧仅保留建议库，右侧仅保留“整理文书”和“下达军令”。
- “整理文书”同时将四节已有玩家文本提交到受限润色接口，回填为可编辑的三国语境浅白文候选稿；接口不读写世界状态、草案、批次或执行结果，且禁止模型添写人物、地点、兵力、资源、条约、战果等事实。
- “下达军令”为唯一不可逆主行动：统一保存四节、逐条规则校验、创建并颁发批次、执行既有 `PhasedExecutor` 的内政→军事→外交→民生→月度核销，完成后进入每月总计。未新增平行 AI、随机性或世界结算入口；对应 AI 文本接口边界已同步写入 `AI活世界与随机性架构基线.md`。
- 浏览器只检查了布局与入口，未点击确认下达军令，未保存/修改活存档；后端已重启并确认润色路由注册。
