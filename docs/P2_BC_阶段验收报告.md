# P2-B/P2-C 阶段验收报告

验收日期：2026-07-22。

## 已落地

- 玩家可通过方略批次自由宣称汉中王或称帝；不符合盘面不会被拦截。
- 规则层按领土、名分、天下口碑、刘备状态与军政危机计算“名实相符／存疑／僭越”。僭越会留下 `political_claims` 审计、降低国势与口碑、影响派系支持，并累积外交压力。
- 称号只能在批次推演中写入；页面和 AI 不能直接改身份事实。
- 文书标题读取实际身份，不再只按年份切换。
- 223 年后的规则结局补充三足均衡与偏安守成，并输出 `evidence[]` 事实依据。
- 场景资源注册表和素材清单已建立；缺失百炼场景在开发环境明确标识，不再静默伪装为专属素材。

## 等待 Claude 素材

`docs/P2_场景素材清单.md` 列出的地图、审阅、推演与事件图仍待生成。素材入库后仅需更新统一 token 和注册表状态，并进行五类可读性截图验收。

## 可复现验证

```bash
PYTHONPATH=. pytest -q
cd web && npm run build && node --test tests/*.test.ts
```

结果：后端 `207 passed`；前端构建通过；前端 `25 passed`。
