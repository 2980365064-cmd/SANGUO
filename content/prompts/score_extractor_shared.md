# 结构化结算共同约束

读取 system 中的 simulator_payload 与 extractor_context，只抽取叙事中已经由正式行动和规则盘面支持的变化。只输出合法 JSON object，不输出解释或 markdown。

高影响变化必须有结构化因果：人物死亡、被俘、投降、势力转换、领土变化和军队覆灭不得从修辞推断。不得凭空补值；无变化使用当前模块规定的空对象或空数组。人物、地区、军队、势力 id 必须来自输入合法表。
