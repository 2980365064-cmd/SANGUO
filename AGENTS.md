# 项目开发约束

## SANGUO 五层 UI Design System（所有 UI 任务强制）

当任务新增、修改、重构、评审或修复玩家可见 UI 时，按以下优先级加载项目内 Skill；上层规则与下层冲突时，以上层为准：

1. `sanguo-ui-art-director`：最高优先级，定义 SANGUO 三国历史档案系统的视觉方向与禁令。
2. `frontend-design`：通用构图、排版、视觉层级和非模板化设计能力。
3. `codex-design-skill`：设计思考、实现前规划与实现质量控制。
4. `sanguo-ui-implementation-workflow`：将已批准的设计落实为游戏级 UI。
5. `sanguo-ui-visual-critic`：完成后的商业游戏视觉审核。

执行门禁：先输出（或在用户已明确同意的既有方案中简要确认）“页面身份、视觉目标、信息层级、布局方案、材质方案、交互方案”，再编码。不得以通用组件、普通 Card、Dashboard、现代聊天气泡或实现便利覆盖 SANGUO 美术方向。完成后必须做截图/可视化检查（条件允许时），并用视觉审核 Skill 给出评分、优点、主要问题、修改建议和是否达到商业游戏标准；低于 8/10 则继续优化，除非用户明确限制迭代范围。

## AI 活世界与随机性（强制阅读）

当任务涉及以下任一主题时，**在设计、修改代码、运行实现前**，必须完整阅读：

[`docs/AI活世界与随机性架构基线.md`](docs/AI活世界与随机性架构基线.md)

- AI、LLM、裁决、人物反应、记忆、奏议；
- 随机性、天气、区域状态、事件、战役环境；
- 月度结算、外势行动、外交、情报、天下反应；
- 月报、本月局势、审计、可见性、读档重放。

实现前必须在任务说明或首条工作进度中写明：将复用的既有模块、随机域（如适用）、可写字段边界和测试计划。不得新建平行随机系统、平行外势 AI 系统、孤立人物记忆表，或让 AI/叙事直接写入世界事实。

## 架构基线同步（强制）

凡是**新增、修改、删除、弃用或修复** AI 活世界与随机性规则的任务，必须在同一任务内同步更新
[`docs/AI活世界与随机性架构基线.md`](docs/AI活世界与随机性架构基线.md)。

这包括但不限于：随机域或种子、事件类型与概率、数值上限、月度顺序、AI 权限、禁止字段、外势预算、人物记忆、情报可见性、审计字段、待收口事项、唯一入口和验收标准。

代码、数据库迁移、测试与基线文档缺少任一项时，任务不得标记为完成；如果规则没有变化，也必须在交付说明中明确写“本任务未改变 AI 活世界规则，基线无需更新”。
